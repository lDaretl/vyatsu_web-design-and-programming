import { ToastOptions } from '../types';
export declare const useToastService: (props: {
    position: NonNullable<ToastOptions['position']>;
}) => {
    yOffset: import("vue").ComputedRef<number>;
    updateYOffset: () => void;
};
