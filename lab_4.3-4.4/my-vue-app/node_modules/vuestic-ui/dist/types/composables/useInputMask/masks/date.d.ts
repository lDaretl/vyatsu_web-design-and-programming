import { Mask, MaskToken } from '../mask';
type MinorToken = {
    value: string;
    expect: string;
    static: boolean;
};
export declare const createDateMask: (format?: string) => Mask<MaskToken, MinorToken[]>;
export {};
