const pick = (o, keys) => {
  return Object.keys(o).filter((key) => keys.includes(key)).reduce((acc, key) => {
    acc[key] = o[key];
    return acc;
  }, {});
};
export {
  pick as p
};
//# sourceMappingURL=pick.js.map
